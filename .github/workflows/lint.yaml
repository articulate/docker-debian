name: Lint

on:
  pull_request:
  merge_group:

jobs:
  setup:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
      - name: Find Dockerfiles
        id: scan
        run: echo "dockerfiles=$(find . -name Dockerfile | cut -c3- | jq -R -s -c 'split("\n")[:-1]')" >> "$GITHUB_OUTPUT"
    outputs:
      dockerfiles: ${{ steps.scan.outputs.dockerfiles }}
  lint:
    runs-on: ubuntu-latest
    needs: [setup]
    strategy:
      fail-fast: false
      matrix:
        dockerfile: ${{ fromJSON(needs.setup.outputs.dockerfiles) }}
    steps:
      - uses: actions/checkout@v7
      - uses: hadolint/hadolint-action@06be81baf89a55ffd0e24b8f04a4185738dd3387 # pin@v3.5.0
        with:
          dockerfile: ${{ matrix.dockerfile }}
  lint-complete:
    if: always()
    runs-on: ubuntu-slim
    needs: [lint]
    steps:
      - name: Verify lint succeeded
        run: |
          [ "${{ needs.lint.result }}" = "success" ] || exit 1
